# Internal audit of20.33

- The printed definition is finite generation plus X-c.e. defining
  relations. No decision procedure for equality is requested.
- The countable free product is over all enumerator indices and all
  finite generator ranks. Every program output yields a legitimate
  presentation; no test of consistency, nontriviality, or isomorphism
  type is used.
- Generator alphabets of the free factors are disjoint and have an
  ordinary computable enumeration. Their relator sets are uniformly
  X-c.e., so dovetailing needs X but not its Turing jump.
- The two associated HNN subgroups really are free on the named bases.
  Freeness of the c_i follows from a retraction to F(a,b), independently
  of any relations or torsion in U_X. Hence the associated map is an
  isomorphism, and the HNN embedding theorem applies.
- The orientation t^(-1)q_i t=g_i q_i gives
  g_i=t^(-1)q_i t q_i^(-1), with exactly the stated substitution.
  Substitution eliminates all HNN relators as identities.
- The resulting presentation has precisely three generator symbols
  and an X-c.e. relator set. It contains U_X by the HNN normal form.
- The converse uses finite products of conjugate relators and effective
  word substitution to enumerate subgroup relations; it does not
  assume decidable ambient word problem or membership problem.
- One free-product factor and zero extra relators are allowed in the
  printed quantifiers. The identity relator gives the same quotient
  if a nonempty finite set is preferred.
- No finite computation can certify the universal oracle assertion;
  the explicit enumeration and normal-form proofs are its evidence.
- Mikaelian's prior two-generator theorem provides a second route and
  a better generator bound. Prior standard constructions are credited;
  no new-priority claim or outside verification is made.
