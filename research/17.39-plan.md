# Plan for Problem 17.39

September 12, 2026, 17:03 UTC. Start with complete-candidate count 45.

The question asks for a uniform number of system normalizers whose
intersection is the hypercenter of every finite soluble group. Investigate
an extraspecial odd-p group extended by an involution that fixes one
maximal isotropic subspace and inverts the other and the center.

1. Derive multiplication, conjugation, the center, and every system
   normalizer explicitly. Prove a dimension lower bound and attain it.
2. Check actual matrix groups with GAP: normal Sylow p subgroup,
   Sylow 2 normalizers, all conjugates, and the exact minimum number
   whose intersection is trivial, by finite intersection-state search.
3. Independently build integer coordinate groups. Enumerate involutions
   and their centralizers without importing GAP tables, and compare the
   full intersection-state distributions and explicit witnesses.
4. Inspect the exact Notebook statement, the cited prior result, and
   subsequent primary sources. Distinguish a proof from a priority claim.
5. Write the proof, report, and source audit; freeze a hash-bound packet,
   replay checks, update the portfolio only if the proof survives, and
   commit locally. No pushes, outside communications, or agents.

Use one CPU per check and at most 2 GB GAP workspace. Finite controls
are checks of an elementary proof, not the basis of its infinite claim.
