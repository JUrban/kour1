# 20.122 minimum-order follow-up

September 12, 2026, after local commit 2db413e. The earlier 138-file packet
is frozen as the completed inclusion-minimal stage.

The nine-point affine group Q=F3^2 semidirect D8 has a 2-group point
stabilizer H whose intersection with every other point stabilizer has
order two. Use the permutation wreath product G=C2 wr Q on eighteen
points. If B is a point stabilizer in G, it is a 2-group and has only
nine distinct conjugates B_w, one per two-point block. Put
A=C2_at_origin x H and C=B.

For two conjugates B_u,B_v, if both block indices are nonzero the
intersection with A contains the flip at the origin. If an index is zero,
the intersection contains the nontrivial common stabilizer in H of the
remaining point. Thus no triple intersection is trivial, while setting
one index to zero and the other nonzero gives an involution outside
F(G). The expected equalities are min=Min=A and F(G)=C2^9.

Verification must construct Q and G explicitly, prove nilpotency of A
and B, prove the Fitting subgroup exactly, establish the full conjugacy
orbit of B, and check all 81 intersection cases. Independently rebuild
the permutations and the stabilizer models without GAP. Preserve raw
process outputs and compare every retained family entry. A positive
verification completes (a) and its soluble version without increasing
the problem-level candidate count beyond 45.

The exploratory coordinate-support screen found eleven sufficient hits;
the support pattern [1,6,6] yields the particularly simple point-stabilizer
construction. Its general forced-involution criterion is not needed in
the final proof, which will directly describe these stabilizers.
