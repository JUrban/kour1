# Problem20.21: order768 excluded, lower bound1536

12 September2026. Bounded result; the general question remains unresolved.

The completed search tests all1,090,235 catalogue groups of order768.
Of these11,562 admit a C12 quotient,606 have both C12 and A4 quotient
types, and none has an isomorphic pair of the required normal kernels.
There are2,878 relevant kernels and2,368 pairs across these606 groups.
All eight search subprocesses and their controller exit0 with clean
logs and complete arithmetic coverage of the catalogue identifiers.

Together with the frozen pilot and the audited prior Conder--Maslova
minimality reduction, this excludes every counterexample of order less
than1536. A smallest example would have order48 times a power of2;
the pilot excludes48,96,192,384 and this packet excludes768. No claim
about existence at1536 or any larger order is made.

The final worker checks the abelianization exponent and then excludes
nilpotent ambient groups, since their quotients cannot be A4. It applies
no further minimality or characteristic-subgroup filter. Every surviving
group has all normal subgroups of index12 enumerated. The earlier slow
order768 run was intentionally stopped and preserved; it contributes
no coverage to this result. The successful fast two-shard control at
order96 exactly matches the pilot's rows and counts.

For an independent check of nonisomorphism, every one of the2,878 actual
kernels from the606 ambient groups was exported as a full multiplication
table of order64. The Python checker validates group axioms, computes
element orders, generates the derived subgroup from all commutators,
and computes the exponent of the abelianization from the quotient table.
It does not use GAP's kernel isomorphism identifiers as nonisomorphism
witnesses. Of the2,368 pairs,2,341 have different element-order spectra
and the other27 have different abelianization exponents. Thus every pair
has an explicit independent isomorphism invariant separating it.

Exact duplicate tables are memoized by their full4,096-byte contents,
not by a potentially colliding hash. There are2,267 distinct tables;
594,280,448 associativity triples are checked. All exported kernel
identifier multisets agree with the original search rows as an additional
coverage check. The46,165,430-byte table stream is stored as a7,824,987-byte
gzip archive, with both compressed and uncompressed SHA256 values.

The first table-check invocation began before the compression process
finished and was correctly rejected with FileNotFoundError and exit1.
Its log is retained. After the compressor actually completed with exit0,
the unchanged checker was rerun successfully. This was an orchestration
failure, not an accepted or silently repaired mathematical computation.

Limitations: completeness of the ambient catalogue and its normal-subgroup
enumeration, together with classification of the quotients as C12 or A4,
still rely on GAP. The pilot's25 independent ambient multiplication-table
controls remain available; this extension verifies all exported kernels
but does not independently reconstruct all1,090,235 ambient groups or
their normal-subgroup lattices. The prior minimality reduction is proved
in the separately archived source audit. All conclusions retain these
explicit computational dependencies and no novelty claim is made.

The packet auditor checks the pilot, all final shard records and hashes,
the compressed table archive and the independently computed kernel
invariants. Replay from the repository root:

```
python3 scripts/audit_20_21_order768.py
```

The complete-candidate count remains41; no outside acceptance is claimed.
