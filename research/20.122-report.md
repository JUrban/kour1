# 20.122: inclusion-minimal counterexample and finite pilot

September 12, 2026.

**Outcome:** a complete elementary negative candidate for 20.122(b),
already in a soluble group and hence also for the corresponding assertion
in (c). The group is S4 x C2, of order 48. The three inputs have orders
16,8,8 and types D8 x C2, D8, C4 x C2. Their complete family consists of
five subgroups of order two and one cyclic subgroup of order four, all
minimal by inclusion. The two generated subgroups have orders 8 and 16:
`min=F(G)<Min=A`.

Start with `research/20.122-proof.md`, which provides explicit
permutations, the complete nine-entry intersection table, and proofs of
nilpotency, solubility, minimality and the exact Fitting subgroup. The
proof is independent of all software. The minimum-order question (a),
also under solubility, remains unresolved.

## Computation and independent checks

The bounded screen completed the following entire GAP catalogues. The
families column counts A distinguished and B,C unordered, with each
slot restricted to conjugacy classes outside F(G). Nilpotent ambient
groups and inputs contained in F(G) satisfy both assertions immediately.

| Order | Catalogue groups | Retained families | Inclusion failures |
|---|---:|---:|---:|
| 24 | 15 | 1,040 | 0 |
| 36 | 14 | 343 | 0 |
| 48 | 52 | 91,795 | 4 |
| 54 | 15 | 177 | 0 |
| 60 | 13 | 513 | 0 |
| 72 | 50 | 17,949 | 0 |
| 96 | 231 | 17,999,133 | 605 |
| 108 | 45 | 13,164 | 0 |
| 120 | 47 | 26,525 | 0 |
| 144 | 197 | 1,550,603 | 32 |
| 162 | 55 | 13,961 | 0 |

Totals are 734 catalogue groups, including 149 nilpotent groups and
585 exported groups; 8,096 active classes; and 19,715,203 families.
Of these, 15,425,621 have a directly witnessed trivial pair intersection,
so the triple family has the identity as its unique minimum.
The remaining 4,289,582 families were explicitly enumerated, yielding
5,737,797 distinct intersections counted separately per family.
There were 641 inclusion-failure families in 15 groups and no
minimum-order failures. The explicit-family counts of minimum-order
and inclusion-minimal members are 4,324,362 and 4,325,116 respectively;
these two counts exclude the shortcut families.

Catalogue and subgroup-conjugacy completeness remain imported GAP
claims for this exploratory screen. They are not needed for the
counterexample. No assertion that order 48 is globally smallest is made;
the listed orders do not cover every smaller group order.

The permutation proof check independently reconstructed all 48 group
elements, 2,304 products, 110,592 associativity triples, all 98 subgroups,
nine normal subgroups and four normal nilpotent subgroups. It found
derived-series orders 48,12,4,1 and upper-central-series orders
1,4,16 for A; 1,2,8 for B; and 1,8 for C. Its full 2,304-pair family
agrees with GAP. The normal closure of the outside element rz has order
24 and is not nilpotent, providing an additional obstruction check.
Five altered certificates are rejected. Both original processes returned
actual exit zero, empty stderr, and their unique completion marker.

`scripts/audit_20_122.py --replay` binds the retained evidence, regenerates
the native model, replays the independent reconstruction, and replays
all eleven supplied-model screens. The packet excludes mutable global
ledgers and its own audit receipt from recursive hash binding.

## Stopped and rejected attempts

The order-192 exporter reached its 180-second wall limit and returned
actual exit 1, without its completion marker. Its partial model, stdout,
stderr and receipt are retained; no screen was launched, and order 192
is excluded from every total. It has not been restarted. The four-worker
controller subsequently returned actual exit zero (collected tool
session 28823, chunk 111d46); its recording marker says ten of its eleven
orders completed. Order 24 was a separate prior successful pilot.
Workers used at most four CPUs concurrently, each with an 8 GiB address
limit and GAP workspace limit 2 GiB.

The first diagnostic invocation incorrectly grouped the GAP option as
`-o2g`; GAP rejected it with actual exit 1. The failed stdout, stderr and
receipt are preserved under `diagnose-grouped-option-failed`. Retrying
with separate `-o` and `2g` arguments succeeded. Neither failure was
treated as a successful calculation. Two local source-download requests
returned HTTP403; the primary text was instead read through the web tool.

## Status and next work

The source and novelty limits are in `research/20.122-source-audit.md`.
Related published pair-intersection examples were found, but the located
text does not state the triple counterexample. Priority is unestablished;
outside reviews remain zero. This adds one candidate, raising the
canonical count from 44 to 45, with (a) still open here.

Further work on (a) should investigate extensions that increase the
orders of the smaller intersections while retaining a minimum outside
the Fitting subgroup, or seek a proof using minimum-order descent.
Ordinary direct products multiply every independent intersection order
and do not by themselves remove the size gap in this example.
