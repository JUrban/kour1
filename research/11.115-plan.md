# 11.115: proper powers and equal commutator subgroups

September 12, 2026, 13:44 UTC. The previous turn completed the 20.100
n=7 packet. The current audit found a possible full counterexample to
11.115 while revisiting its earlier sufficient conditions.

Take X=F(a,b), N the normal closure of a^m for m>=2, and T the kernel
of X onto C_m that sends a to a generator and b to the identity.
The proposed equality is [T,N]=[X,N]. The key point is that [T,N] is
normal in X, since both inputs are normal, and the image of a^m is
central in X/[T,N]. This would refute the exact strict-inclusion claim.

Before counting the result:

1. Check the supplied statement visually, including which subgroup is
   required not to be maximal and whether any extra restrictions occur.
2. Prove all strict containments and the equality without computations.
3. Give the explicit Schreier basis of T and a second description of
   the common commutator subgroup and quotient.
4. Check the finite free-word identities in native GAP and in independent
   reduced-word arithmetic. State that bounded checks do not establish
   the infinite-subgroup equality by themselves.
5. Audit the relation-module sufficient conditions for consistency,
   search for a prior answer, preserve the reading scope, and bind the
   proof and evidence in a local packet.

The separate 20.113 ANUPQ probe reached a class-eight progress marker
but stopped at its explicit 180-second bound with actual exit 1 and no
completion sentinel. Its evidence is retained and is not a solution.
No job from that probe remains live; no automatic restart is planned.
