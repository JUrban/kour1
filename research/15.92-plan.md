# Problem 15.92: plan and completion record

Started 2026-09-11; proof candidate developed during the authorized
48-hour research period. The earlier conditional development is retained
in `15.92-bounded-support-route.md`.

## Objective and standard of evidence

For every integer r>6, construct continuum many nonisomorphic infinite
quotients of the ordinary triangle group Delta(2,3,r), each residually
finite alternating with onto separating maps. Distinguish the general
proof from finite controls, and credit all imported construction results.

## Work completed

1. Read the Notebook question and inspect its presentation visually.
   Record why the missing final '=1' does not affect an affirmative answer.
2. Identify the kernel-independence obstruction: infinitely many finite
   images and continuum many maximal subgroups do not by themselves solve
   the question.
3. Prove a general bounded-support criterion. Use a marked limit and
   its finitary normal subgroup to force large finite alternating maps
   to detect the limit kernel. Construct individual separating witnesses
   recursively and count quotient isomorphism types.
4. Find a precise finite-image input for every parameter. Read Conder's
   Chapter 5 construction, inspect the required diagrams and cycle tables,
   and verify the ordinary subgroup and parity assertions.
5. Derive a fixed word in the ordinary generators whose images are single
   11- or 13-cycles. Check its exponents against all divisors of 720720.
6. Replay the 13 required cycle tables and joins in 150 parameter cases.
   Compare explicit permutation powering with cycle arithmetic. All pass.
7. Reconstruct the two r=7 base diagrams directly from their geometry.
   Preserve the first orientation error and its rejected process record.
   The corrected independent GAP verification covers five joined examples
   using the primitive-group criterion. Four general group-order checks
   also pass; the redundant fifth order calculation is deliberately
   stopped and retained with its actual unsuccessful process status.
   Final completion is required by the packet auditor.
8. Write the complete proof and conduct the 21-point logical audit.
   Record the exact source-reading scope and the limits of the priority
   search. Independent external review remains pending.

## Freeze procedure

Require actual successful process exits, clean GAP output, both completion
sentinels, and all five expected alternating-group controls. Bind the proof,
review, scripts, source inputs, retained failure, and final outputs by
SHA-256 in `results/15.92-summary.json`; run `scripts/audit_15_92.py`.
Only after that audit passes, update the candidate count and commit the
packet locally. The manifest and audit result record final completion.

The GAP controls validate the reconstructed examples, not the infinite
argument or every Conder diagram. The full mathematical conclusion rests
on the proved criterion and the cited all-parameter construction.
