# Plan: normal subgroups of algebra unit groups

September 12, 2026. Extend the positive-characteristic coverage of
20.89 / 19.83 without claiming closure for arbitrary linear subgroups.

1. Use invariance of a minimal sink under automorphisms fixing its
   element. In an algebra, conjugate by the units 1+t*a.
2. Prove that two distinct parameters giving the same conjugate of e
   force e to commute with a. An infinite field and a finite sink then
   force the sink to be trivial.
3. Apply this to normal subgroups of unit groups of finite-dimensional
   algebras, which have faithful regular linear representations. Invoke
   Gruenberg only after showing the elements are Engel.
4. In a finite field prove the quantitative alternative: every
   nontrivial sink has at least one more element than the number of
   parameters for which 1+t*a is invertible. Check this using native
   GAP groups and independently enumerated prime-field matrices.
5. Record the continuing arbitrary-subgroup obstruction and the
   unrelated 20.81 / 20.83 source triage. Preserve actual process
   records, replay the packet, update the status and commit locally.
