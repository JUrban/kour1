# Problem 20.21: order768 extension

12 September 2026. Started after freezing the pilot and auditing
the Conder--Maslova minimality reduction. The next possible order
of a smallest example is768; the Small Groups catalogue has
1,090,235 entries of that order.

Eight GAP processes search distinct residue classes of catalogue
identifiers, each with a1GiB workspace ceiling. Together with the
six existing14GiB certificate verifiers, this reserves92GiB of
GAP workspace, below100GB before overhead; observed actual use
is substantially smaller. There are14 active compute workers,
below the20-CPU limit. Monitor resident memory and do not launch
another large-memory worker until capacity becomes available.

The search uses the same exact abelianization filter and complete
normal-subgroup enumeration as the pilot. Although the audited
reduction motivates this order, no additional minimality filter
is used. Every matching kernel identifier is checked with an
explicit GAP isomorphism. The raw data retain every group with
both quotient types. The controller records actual subprocess
returns, checks clean logs and final sentinels, and verifies the
arithmetic partition and every retained row before aggregating.

A two-shard replay at order96 reproduces the pilot's full data:
231 groups,31 eligible groups, four groups with both quotient
types and zero matching pairs. Its first attempt was rejected:
GAP requires a range endpoint congruent to its starting point,
and the initial second shard used a noncongruent endpoint. GAP
printed an error despite returning0; the controller correctly
returned1. Both initial scripts and all initial output are kept.
The corrected endpoint rounds down to the required residue class.
The successful retry has controller exit0 and exact equality with
the archived order96 rows.

The order768 job has no proof status while running. If it finds
a candidate, export full kernel and ambient data for independent
verification before making any assertion. If it finishes without
a candidate, audit all shard exits, rows, counts and hashes, and
extend the independent table controls as needed. Only then can
the minimality reduction raise the lower bound to1536. Keep the
complete-candidate count unchanged unless a full resolution is
proved. All work remains local, with no pushes or messages.
