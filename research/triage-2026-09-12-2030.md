# 20:30 UTC checkpoint and remaining deadline work

12 September 2026. The active 48-hour research window ends at
20:56:46 UTC. The canonical complete-candidate ledger remains at
46, with zero outside reviews and no established priority.

The last research pass added four partial packets for 21.121:

* `a6ff4b2`: sharp exponent-two bounds for soluble and normal-Sylow
  cases, with native and independent finite controls.
* `db45c67`: exact attained exponent log(60)/log(4) for the restricted
  power of A5; all 8,381 two-factor subgroups independently reconstructed.
* `acba103`: characteristic p-Jordan bounds at the same exponent,
  finite-index permanence, and finite p-kernel permanence.
* `4ffd8d9`: all finite normal quotients preserve the admissible
  exponents when the ambient group is generalized p-Jordan; in
  particular this holds within the p-Jordan class. The proof uses
  the class-two centralizer of a finite kernel. No new finite verifier
  was claimed for the two short permanence deductions.

These do not resolve the universal upper bound in part (b). The
new finite controls are all terminal and accepted; old packet
dependencies were preserved. A5 remains a countercheck against
extending the soluble J^3|P|^2 bound without its hypothesis.

The final research report has been drafted and committed in
`reports/FINAL_REPORT.md`; it is explicitly still a draft pending
the deadline observation. `research/closeout-proof-review.md` records
complete rereads of nine candidate arguments and two 20.89 partial
proofs. No new mathematical gap requiring withdrawal was identified.
The general positive-characteristic 20.89 argument still cannot use
Shumyatsky's all-elements-almost-Engel theorem before closure is
proved. The same local-property/common-kernel obstruction remains.

The final-closeout preflight now passes after adding three unchanged
GAP/SmallGroups source files that were present but not tracked.
It checks 4,231 bindings, 59 manifest entry points, 2,239 distinct
files, 88 local links, and the 46-row candidate table. It sees no
mathematical jobs running. See `reports/closeout-preflight-report.md`.

Remaining authorized work:

1. Continue useful bounded proof review and triage through the
   remaining window; do not start another long exhaustive search.
2. At the deadline replace the explicit draft wording in the final
   report, update STATUS and PORTFOLIO to closeout, and retain every
   partial-result and priority qualification.
3. Invoke `scripts/audit_final_closeout.py --phase final` with a
   fresh output path under `results/final-closeout-`. Preserve
   stdout/stderr and the actual process exit in a separate receipt.
   Its deadline guard deliberately prevents an early final PASS.
4. Bind the final report, status notes, successful final audit,
   receipts, auditor and entry-point packets in a final manifest.
   Then verify exact staging and commit locally. Do not modify the
   report after freezing it merely to insert its own commit hash;
   use the Git commit and final response for that identifier.
5. Observe that the requested window and deliverables are complete,
   then mark the goal complete. Do not mark it complete before the
   deadline merely because a draft report exists.

Nineteen unrelated historical working files remain untracked and
must be preserved. There is no live computational session to wait
on after the preflight's terminal observation. No push or external
message is authorized or needed.
