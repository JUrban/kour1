# Problem 13.42: result and exact verification

The unrestricted Z[t]-completion of the free nilpotent rank-two,
class-three group is not locally nilpotent. An explicit torsion-free
Z[t]-group model sends [x^t,y,...,y] to the nonzero polynomial (T-1)^r
in the additive rational group algebra Q[Q]. The scalar action uses
only a polynomial's constant and linear coefficients and is checked
against all four original exponential-group axioms. This is candidate
36, pending independent review and further priority work.

## What proves the infinite statement

The proof supplies formulas for the entire target group and its scalar
action. The universal property sends the free nilpotent source into
that group and extends the map to its completion. A coefficient of
one at T^r proves nonvanishing for every r. No finite search or cited
structural non-nilpotency theorem is substituted for this argument.

The original authors' axioms and universal-property definitions are
the only specialized prior input. The rational nilpotent coordinate
group is realized by finite exp/log formulas in a truncated free
associative algebra. The category is unrestricted: neither Hall's
completion nor a completion within nilpotent A-groups is substituted.

## Verification

The Python check performs a generic symbolic expansion in ten commuting
coordinate variables and the 15 noncommutative words of lengths zero
through three. It verifies the product law against multiplication of
finite exponentials; the resulting expression has 81 nonzero scalar
monomial coefficients. It also verifies the inverse, conjugation
coordinates and all three minors in the commuting criterion.

Exact rational controls on 75 elements and ten integral polynomials
then verify 7,500 additions of exponents, 7,500 products of exponents,
750 conjugation identities and 1,875 compatibility identities for f.
There are 525 verified commuting pairs, with 5,250 checks of the
conditional product axiom. Additional inverse and associativity
controls pass. The first 64 iterated commutators agree with their
integer binomial coefficients and have nonzero leading coefficient.

The control process completed with actual exit 0 and its completion
sentinel. Only exact rational arithmetic is used. No new GAP worker
was started for this packet: the generic algebra calculation provides
the independent coordinate check, and the six long n=7 workers were
left running.

## Sources and scope

Myasnikov--Remeslennikov, *Exponential groups 2*, author PDF:
https://web.stevens.edu/algebraic/alexeim/Publications/All_files_new/expo2jj_29.pdf .
Definitions 1 and 2 and their introductory context were read; pages
1 and 3 were viewed. The PDF, text and those images are retained,
along with the exact Notebook question on page 69. No later theorem
about CSA groups is needed or claimed as audited.

Amaglobeli--Miasnikov, arXiv:2601.17600v1 (24 January 2026), was
checked for its introductory scope and definitions. It studies
restricted class-two completions and does not provide the construction
above. Its full proof is not an imported premise. Searches remain
limited, so the result is not certified as new.

The explicit closed manifest and `audit_13_42.py` bind the proof,
internal review, exact model, symbolic check, original output and
source files. The auditor replays the controls. All work is local;
there have been no external messages or Git pushes.
