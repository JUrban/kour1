# 20.89 / 19.83: partial proof plan

September 12, 2026. The two numbers ask the same question. Neither is
counted as a new complete solution in this packet.

1. Fix the exact finite-sink quantifier: one finite set for every input,
   although the time to enter it may depend on the input.
2. Prove a scalar-action lemma for an additive subgroup of a field.
3. Use the individual matrix-entry filtration of a triangular group's
   unipotent subgroup to characterize its almost Engel elements.
4. Prove the finite-or-central-series implication needed for sufficiency,
   recording the classical Hall and Baer inputs explicitly.
5. Test the filtration in actual finite matrix groups and independently
   check a mixed example with a fixed nonnilpotent finite kernel and an
   additive layer of growing dimension. Finite groups themselves cannot
   distinguish almost Engel elements: all their elements are almost Engel.
6. Record the residual-finiteness reduction separately. Do not infer
   uniform sinks in an arbitrary ambient group from sinks in its finitely
   generated subgroups.
   Apply the reduction to finitely many conjugates of one almost Engel
   element to show that its minimal sink generates a finite subgroup;
   deduce the torsion-free linear case.
7. Preserve sources, process return codes, hashes and replay instructions;
   commit the partial result locally. General linear groups remain open.

No sub-agents, outside messages or pushes are authorized or used. These
checks use one process at a time and are well below the resource limits.
