# Problem 18.77: alternating forms and the small-class case

12 September 2026. General problem remains unresolved.

The initial Pfaffian idea for exponent-p class-two groups extends to
alternating forms over Z/p^k. If the largest image order is p^(2e),
where e<p, the parameters attaining it generate the whole parameter
group. The key point is to normalize a maximal form into r nonzero
symplectic blocks, with r<=e, and use its degree-r Pfaffian on a pencil.
An index-p subgroup containing every maximum parameter would give
p-1 roots and a vanishing leading coefficient after reduction modulo p.
Interpolation also controls division by the minimal p-adic valuation.

Prove this algebraic lemma in full. Apply it to the Kirillov correspondence
for all finite p-groups of class less than p, without an exponent-p
restriction. Also give the elementary class-two character-theoretic
application, using alternating commutator bicharacters. Both are partial
answers only; no new complete candidate is counted.

Read the exact primary orbit-method statement and distinguish it from
an audit of the full imported proof. Check the ring lemma on exact
alternating matrix pencils over several prime-power rings, including
sharp examples at e=p. Retain all source, process and manifest evidence.
No broad GAP character-table screen is needed for this structural route.
