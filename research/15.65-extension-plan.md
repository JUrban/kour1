# Completing the remaining components of 15.65

11 September 2026, 19:34 UTC. Active extension; the committed unitary
cyclic proof remains valid and separately audited.

The cyclic symplectic product has the same factor A(t) as the unitary
one, with exponent (1/t+1-e)/2, e=1 or2 according to characteristic.
At alpha=(1-sqrt(5))/2 this is (alpha-e)/2, again irrational. Every
other factor is nonzero near alpha. FNP Theorem2.3.11 gives orthogonal
cyclic limits as nonzero rational multiples of the symplectic limit.
This should settle all cyclic components, separately in both parities.

For the Notebook's literal S (minimal polynomial squarefree, i.e.
semisimple in FNP), set

    U(t)=sum_(m>=0) t^(m^2)/product_(i=1..m)(1-(-t)^i),
    L(t)=sum_(m>=0) t^(m^2)/product_(i=1..m)(1-t^i).

FNP Lemma3.1.9 and Theorems3.1.13,15,18 express every semisimple limit
using (1-t^d)U(t^d) and (1-t^d)L(t^d), with the same polynomial counts,
and ordinary analytic prefactors from symplectic/orthogonal orders.
U(-1/2)>0 and U(-3/5)<0 by alternating-series bounds. The first
negative zero beta is therefore in (-3/5,-1/2). For |u|<=3/7 the
series defining U and L differ from one by less than one, giving a
zero-free disc for all higher-degree factors near beta. The sole
remaining degree-one L(t) factor is positive on [-3/5,0] by another
alternating-series estimate.

A local zero of U has finite positive multiplicity. Its exponent
N_1(1/t), or N*(1/t;2), is nonconstant. Going once around that zero
multiplies the continued function by exp(2*pi*i*m*P(t)). This cannot
be identically one on an open set. All other local factors are
single-valued meromorphic, so no rational interpolant can exist.
The analytic prefactors are not identically zero (their values at
zero are positive); finite-order zeros cannot remove this monodromy.

Needed before claiming the extension:

1. Check every source formula and characteristic convention visually.
2. Prove the zero-free bound, sign tests, all tail convergence, and the
   local monodromy lemma without relying on numerical approximations.
3. Verify finite unitary/symplectic semisimple counts and the new formal
   series against the source tables and actual GAP groups.
4. Record source reading limits and a separate internal audit. Keep the
   original 15-file packet frozen; bind the extension in its own packet.

No additional candidate count is intended: this completes more of31.

## Completion at 2026-09-11T19:52:25.821478+00:00

All four planned steps are complete. The proof and internal review are
`15.65-classical-proof.md` and `15.65-classical-review.md`. The exact
control packet covers23 groups,563 classes,69 finite probabilities, and
117 comparisons with a second series implementation. One published
coefficient discrepancy was isolated and independently explained; it is
retained explicitly. Source-page images and both initial process records
are retained. Candidate31 now covers all stated components; the count
remains31 and outside review and novelty checks remain pending.
