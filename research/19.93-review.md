# Problem 19.93: internal review

12 September 2026. Same-agent review, not independent acceptance.

1. The notebook page says epimorphic images of G. Replacing this by
   quotients of arbitrary subgroups would change the problem and would
   invalidate the proposed exclusion. Both the text and image were checked.
2. The map rho from the wreath product onto F_p^2 is explicit. It is
   enough that it is onto; no Frattini-subgroup identification is needed.
3. A nonzero top coordinate acts as a p-cycle, so the norm is precisely
   the coordinate sum. Zero top coordinate is handled separately.
4. Independent points on the union of the two axes must occupy different
   nonzero axes. Their sum has both entries nonzero, excluding (uv)^p=1.
5. In a noncommutative algebra, (X+Y)^p is not X^p+Y^p. All binary
   length-p words occur. This proves that the third relation has initial
   degree p; the checker verifies the exact expansion.
6. The three algebra relations are not homogeneous. The imported
   inequality includes division by 1-t, exactly as in Ershov Theorem 2.6.
   Evaluation at 2/3 is only under a temporary convergence assumption.
7. The ideal is closed and contained in m^p. The quotient is separated;
   its first two dimensions remain 1,2. No unproved completion/group
   algebra identification is used.
8. The finite truncation's nilpotent ideal makes 1+M_N a group of
   p-power cardinality. The two generators satisfy all three exact
   power relations, not just their initial homogeneous terms.
9. The additive map to A_1/A_2 proves that the minimal generator number
   is exactly two. N=2 is included; N=1 is used only for a dimension bound.
10. The image of the finite group algebra contains X,Y, which generate
    the truncated algebra polynomially. Augmentation powers map into
    powers of the nilpotent ideal, establishing the required surjection.
11. The finite lower p-central series terminates by induction through
    the centre. Every factor is elementary abelian, so the chosen lifts
    have unique ordered normal forms with exponents from 0 to p-1.
12. Expanding each power in that fixed order proves spanning without
    commuting any factors. Only spanning is needed for the dimension bound.
13. The augmentation weight inclusion uses both h^p-1=(h-1)^p and
    the displayed commutator identity. The inequality ip>=i+1 holds
    for every p>=2 and i>=1.
14. High-weight products vanish modulo I^N. Extra cancellations among
    low-weight products can only improve the upper bound; independence
    or an exact Jennings dimension-subgroup formula is not assumed.
15. The infinite partition product converges for every 0<t<1. Bounds
    on cumulative dimensions imply H(t)<=P_K(t)/(1-t), which suffices
    for the contradiction; coefficientwise H<=P_K is not claimed.
16. The prime and generator count stay fixed while N varies. Existence
    for every K contradicts a single finite value f(7,2). Finite computations
    supply corroboration, not the quantifier over all K.
17. The GAP quotients are quotients of the abstract triangle group.
    They are not identified with the particular truncation groups Q_N.
    They test the same relations and lower-series convention only.
18. The argument claims p>=7. The test at p=5 is positive and is retained
    to prevent extending the stated GS computation to p=5 accidentally.
19. Initial GAP global-scope warnings were rejected despite exit zero and
    a sentinel. The final function with local variables has a clean log.
    The Python finite truncations are eliminated in both degree orders.

No internal gap identified. The filtered GS theorem remains imported;
the bounded controls do not certify it or establish priority. Exact
literature searches found no prior resolution, which is not a novelty proof.
