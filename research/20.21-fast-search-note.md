# Problem20.21: exact nilpotency filter and preserved restart

12 September2026. The initial order768 run was intentionally
interrupted after about12 minutes. Its outputs are retained and
have no completed-search status. A diagnostic of the first32
catalogue entries showed that they are nilpotent, admit C12, and
are cheap to construct. Enumerating their normal subgroups was
unnecessary for the two-quotient question.

The replacement worker keeps the same eligible counter and then
skips nilpotent groups before normal-subgroup enumeration. This
filter is rigorous: a quotient of a nilpotent group is nilpotent,
because its lower central series is the image of the original
one. In A4, the commutator subgroup is V4 and [V4,A4]=V4, so A4
is not nilpotent. A nilpotent ambient group cannot be an example.

No minimal-counterexample hypothesis is used by this filter.
The search still visits every catalogue identifier and compares
all required kernel pairs in every group that survives the two
necessary filters. The count of groups admitting C12 is unchanged.

The new two-shard order96 control exits0 and reproduces exactly
the frozen rows and counts:231 tested,31 eligible, four with both
quotient types, zero matching kernel pairs. The initial range
failure, the corrected but slower control, and the interrupted
order768 run are all preserved under their original names.

The replacement command is

```
python3 scripts/run_20_21_fast.py 768 --shards 8
```

It writes files with the prefix `results/20.21-order768-fast`.
Eight1GiB workers again keep the combined GAP workspace ceiling
at92GiB with the six14GiB certificate verifiers. This launch has
no result until all eight workers and the aggregation finish.
The earlier launch plan remains an accurate historical record.
