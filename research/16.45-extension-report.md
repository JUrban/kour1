# Problem16.45: completed bounded extension

12 September2026. Bounded sufficient certificates, no new resolution.

All2,435 groups of orders81,125,128,243,343,625 have a subgroup of
maximum generator rank whose Frattini subgroup is normal in the
ambient group. The exact catalogue counts are15,5,2328,67,5,15.
The same sufficient-condition proof in16.45-plan.md therefore gives
b(G)=mu'(G) in every case, including nonfaithful actions.

The extension covers619,438 conjugacy classes of subgroups,
representing1,108,458 actual subgroups. There are15,257 classes of
maximum generator rank:5,376 are abelian and15,128 have normal
Frattini subgroup. These sets overlap. In1,010 groups there is no
abelian witness of maximum rank, so the second certificate is needed.
Every group has a normal-Frattini witness; no unresolved row remains.

Eight independent table controls cover all six new orders:
(81,14),(125,3),(128,133),(128,990),(128,163),(243,19),(343,3),(625,14).
The checker reconstructs474 subgroups, verifies307,619,161 associativity
triples, computes all Frattini intersections and subgroup generator
ranks, and gives explicit irredundant stabilizer families. Its subgroup
counts and maximum ranks agree with the GAP screen. The table checker
is the frozen pilot algorithm copied with only the input/output paths
and completion sentinel changed; the pilot remains unchanged.

The screen ran for1613.97seconds in one GAP process with a4-GiB
workspace ceiling and3600-second timeout. It exited zero, without
warnings or errors, and printed its final sentinel. The separately
observed outer runner also exited zero. Export and table controls
then exited zero, with clean sentinels; the controls took28.38seconds.
The screen, table export, exact catalogue counts, checker, outputs,
logs, process records, observations and report are bound together
with the original pilot manifest. The final audit replays all20
independent table controls, including the12 pilot controls.

Combined with the disjoint354-group pilot, this establishes the
sufficient condition for2,789 groups and enumerates1,153,563 subgroups.
The20 table controls independently reconstruct3,717 subgroups and
verify308,765,820 associativity triples. These are bounded results.
The general16.45 question and a proof for all p-groups remain open here.
