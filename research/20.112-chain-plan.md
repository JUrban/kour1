# Independent subgroup-chain audit for 20.112

2026-09-12. This is bounded verification, not a proposed solution.

Recheck the twelve groups retained by the earlier N^3 screen, and their
Frattini quotients, using maximal-subgroup chains. An edge M<K is accepted
when K/Core_K(M) has a Fitting series of length at most three. This does
not call the screen's iterated nilpotent-residual criterion. Negative
controls use S3 with N and S4 with N^2; S4 with N^3 is a positive control.

For each positive case, export a faithful permutation model, the Cayley
graph for its generators, Sylow and normalizer generators, the chain,
each core and a lifted Fitting series. A separate Python checker will
reconstruct the group from permutation multiplication and verify the
subgroups, normalizers, cores, maximality and nilpotent factors directly.
The original catalogue and GAP's group identification remain imported.
Positive certificates do not require trusting the maximal-chain search.

Preserve the original screen and its hashes. Require actual successful
process exits, empty stderr and terminal sentinels. Bind the completed
evidence and exact scripts in a new manifest, then commit locally.

The installed catalogue has no groups for the queried next orders
2592,2916,3888,5184,5832,7776,10368. No search at those orders is claimed.
The ongoing n=7 verification workers are left running.
