# Problem 19.48: independent example and prior resolution

The answer is negative. Our explicit example over F9(t)/F3(t) has
all levels free of rank two over F3[t]. A nine-factor word produces
the new upper transvection with parameter 2+j, j^2=-1. The complete
proof is in `19.48-proof.md`, with every prefix in `19.48-word-table.md`.

During priority checking, a September 1 preprint giving a different
characteristic-zero example was found. Its two-page proof was read and
its exact identities independently checked. Accordingly **19.48 does
not increase the new-candidate total**. The prior-answer source and
scope limits are in `19.48-source-audit.md`.

The initial finite-field word check, GAP check and R-module basis
controls all returned zero. The extended checker also verifies the
prior cubic identities by 6-by-6 integer matrices. There are 3,024 basis
product checks for ranks 3 through 8 and three rejected corruptions of
the explicit word. The initial checker and its results are retained
under `results/19.48-initial-controls/` after the prior controls were added.

The written ideal argument covers every rank, and R-bilinearity explains
why basis products suffice for the individually checked ranks. There is
no extrapolation from enumerated groups in the proof: the nine-factor
identity directly produces the forbidden transvection.

Reproduce the frozen packet from the repository root with:

    python3 scripts/audit_19_48.py

It checks all hashes and actual process records, regenerates the word
and table, repeats the independent controls, and replays GAP. A
separate observation records the auditor's actual exit and required
`PASS_1948_PACKET` sentinel. The characteristic-three example's separate
priority remains unchecked; it is recorded as an independently verified
alternative, not a new answer to an unresolved problem.
