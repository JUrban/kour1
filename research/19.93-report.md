# Problem 19.93: complete negative candidate

12 September 2026. Candidate, pending independent review and novelty checks.

There is no proposed function f: already for (p,d)=(7,2), finite
p-groups without a C_p wr C_p quotient have unbounded lower p-central
factor orders. The same construction works for each prime p>=7.

The three relations x^p=y^p=(xy)^p=1 prevent a wreath quotient by an
elementary calculation in its two-dimensional quotient. Finite
truncations of the corresponding complete algebra yield two-generator
finite p-groups. The filtered Golod--Shafarevich inequality gives
divergence at2/3. A direct normal-form count shows that bounded lower
p-central widths would force convergence at every point below1.
The proof avoids a dimension-subgroup theorem and any identification
of a completed triangle-group algebra. See `19.93-proof.md`.

Exact Python controls check21,714 wreath norm identities, exhaustively
for p=2,3,5 and with3,000 seeded elements each for p=7,11. All possible
axis images are checked; removing the third relation is a retained
mutation that allows the standard wreath generators. Twelve algebra
truncations, through degree cutoff12, have their relation-span ranks
checked by elimination in both opposite degree orders. Their Hilbert
prefix is1,2,4,8,16,32,64,125,247,488,964,1904. The exact GS value is
-115/729; the same evaluation at p=5 is positive.

GAP computes seven triangle7 quotients of p-classes2--8. The largest
has order7^68 and lower p-central dimensions2,1,2,3,6,9,17,28.
Independent actual permutation wreath groups of orders8 and81 have
respectively8 and648 generating ordered pairs of elements of order
dividing p; every such product has order p^2. These controls do not
establish unboundedness and are not asserted to equal the groups Q_N
in the proof.

Two initial GAP runs produced global-scope warnings. Their logs and
the rejected process record remain archived. The second wrapper
actually exited1 after rejecting the warnings, even though its GAP
child exited0. The final local-variable implementation has a clean
log and its process wrapper actually exited0 (tool chunk58fb3d,
child runtime2.310s). Python controls actually exited0 (chunkd617c6,
child runtime0.482s). These are finite controls, not automated theorem
verification. The integrity/replay audit is recorded separately in
the summary and audit-observation JSON files.

The original page and Ershov's relevant pages were viewed. Nineteen
specific internal review points are recorded. No internal gap was
identified. The classical filtered GS theorem is the sole substantial
import; a full external review and priority check are still required.
