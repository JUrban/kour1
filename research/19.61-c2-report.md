# Problem19.61: complete five-ring C2 pilot

12 September2026. Bounded negative search; general question unresolved.

Every elementary carpet was tested in the standard simply connected
typeC2 matrix group over each of five small rings. No closure failed
the carpet conditions.

| Ring | Additive subgroups | Assignments tested | Valid carpets | Larger closures |
|---|---:|---:|---:|---:|
|F2|2|256|64|0|
|F4|5|390625|2515|576|
|F2[t]/(t^2)|5|390625|6717|936|
|F2 x F2|5|390625|6915|1536|
|Z/4|3|6561|1398|0|
|Total|||17609|3048|

The preparation program constructs the eight standard symplectic root
matrices, checks their root-subgroup laws and preservation of the
alternating form, and factors every relevant pairwise commutator by
exact ring arithmetic. Unique matrix factorization determines each
individual Chevalley root factor, including signs and coefficients2
over Z/4. It then enumerates all additive subgroups and all assignments
satisfying these implications. Opposite roots are excluded from this
commutator formula. The ambient permutation action on all ring vectors
is faithful, since it includes the basis vectors.

GAP generates each carpet subgroup and tests every root element for
membership. The resulting additive subgroups are checked against all
carpet implications. Every enlargement is retained, including the
initial carpet, its closure and the generated group's order. A larger
closure is not itself a counterexample: the question asks whether that
closure remains a carpet, which all3048 enlargements do here.

Independent Python controls enumerate generated groups as actual
matrices, using neither GAP membership nor its permutation data. They
cover all64 F2 carpets, every carpet supported on at most two roots,
and a representative enlargement for every observed group order in
each ring. All1288 controls agree; they include104 enlargements and
enumerate134,213 matrix states in total. The largest selected group has
order36,864. The hard cap of50,000 states was never reached; a cap
would abort the check rather than create a passing control.

An eight-element mutation uses only the root groups for a short simple
root and its adjacent long simple root over F2. Its root intersections
do fail the carpet conditions, but its initial assignment already fails
them. This verifies that dropping the initial-carpet hypothesis would
produce a false counterexample to19.61. The control explicitly rejects
that interpretation.

All preparation, search and control executions have observed exit0,
clean logs and final sentinels. Four search child exits are recorded by
the inline subprocess controller; the separate F2 exit and other tool
observations are recorded explicitly. The packet auditor checks hashes,
regenerates all five GAP input files in a temporary directory, checks
coverage and output counts, and replays the independent matrix controls.
The first replay was rejected solely because Python's integer histogram
keys serialize as JSON string keys. The corrected comparison normalizes
the replay through JSON; the initial auditor, manifest and failure log
are retained. No mathematical output was changed by that correction.

Source scope: Troyanskaya2024 is recorded in the plan and cached. Its
mathematical text was read, including the elementary B2 odd-characteristic
argument; the G2 calculation is not independently certified. No theorem
from it is needed for the five-ring computations. No source page image
was viewed for this pilot. The group model is the displayed standard
symplectic matrix realization, not an unspecified central quotient.

This packet supplies finite evidence only, with no novelty assertion.
It does not settle other rings, root systems or the general question.
The complete-candidate count remains41.

Replay from the repository root:

```
python3 scripts/audit_19_61.py
```

To regenerate raw searches, use a separate checkout because output
files are deliberately protected or overwritten according to their
individual scripts. The preparation rejects existing input files.
