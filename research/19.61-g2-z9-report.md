# Problem 19.61: all G2 carpets over Z/9

12 September 2026. Bounded affirmative result in the frozen integral
adjoint Chevalley matrix model. The general problem remains open.

There are exactly **20,047** carpets over Z/9, and every one is already
closed. Thus this ring supplies no counterexample to 19.61. The result
does not increase the complete-candidate count.

## Exhaustive input

The additive subgroups are 0, 3R and R. The preparer examines all
3^12 = 531,441 assignments to the twelve roots. It computes each
nonopposite-root commutator as an actual 14 by 14 matrix and uniquely
factors it in the ordered root-string product. All parameters are
enumerated, including nonunits. The resulting implications are checked
on each assignment; exactly 20,047 survive. The integral root matrices
and their Lie-algebra checks are bound to the preceding G2 prime-field
packet. Identification with the standard Chevalley construction remains
an imported mathematical input. No permutation-action faithfulness
claim is needed for the new calculation.

## Exact kernel argument

Write pi for matrix reduction modulo three. Its kernel in GL14(R) is
the elementary abelian group

    U = { I + 3X : X in M14(F3) },
    (I+3X)(I+3Y) = I+3(X+Y).

For a carpet, call a root full if its parameter subgroup is R. Let H0
be generated by x_r(1) for the full roots, and put B = pi(H0). Reducing
the carpet conditions gives one of the 217 frozen F3 carpets. The
single all-full assignment is already the full carpet and needs no
enumeration. Every other B is enumerated as actual matrices, retaining
one lift t_b in H0 and its actual inverse for each b in B. The orders
agree with all 216 preceding proper prime-field cases: 109,693 states
in total, each group of order at most 5,832. The enumeration separately
checks that x_r(1) modulo three lies in B precisely for the full roots.

For every generator s and state b, compute the discrepancy

    t_(b pi(s))^(-1) t_b s = I+3 X_(b,s).

Let W0 be the F3-span of these 696,168 edge discrepancies. Each belongs
to H0 intersect U. The code checks that W0 is invariant under conjugation
by every generator and that it contains the vectors of their third
powers. Invariance is tested on a basis. Inverse-generator invariance
also implies forward invariance because this is a finite vector space
and conjugation is invertible.

Here is the completeness argument, including the discrepancy orientation.
The union of cosets t_b (I+3W0) contains the identity and is stable under
right multiplication by every generator: move the kernel element past
the generator using invariance, and then use the recorded edge
discrepancy. Since the ambient matrix group is finite, positive words
in the generators comprise H0. All the cosets already lie in H0, so
their union is exactly H0. Its reduction kernel is therefore I+3W0.
This proves equality without assuming any Lie-algebra dimension or
smoothness theorem, or an incorrectly oriented Schreier formula.

The remaining initial generators are x_r(3) for roots whose parameter
subgroup is 3R. They all lie in U. Let v_r be their kernel vectors.
The normal closure of these extra generators, together with I+3W0,
is exactly I+3W, where W is their H0-invariant linear span with W0.
Indeed U is abelian, so conjugation by the extra generators acts
trivially on U; conjugation by H0 supplies every required conjugate.
It follows that H intersect U = I+3W for the whole carpet group H.
The code precomputes W0 plus the invariant span of each individual v_r;
adding the relevant spaces gives W for each carpet.

A root that was not full cannot acquire any unit parameter: reduction
would put x_r(1) in B, contrary to the complete modulo-three membership
check. Its closed parameter subgroup is consequently 3R exactly when
v_r belongs to W, and is zero otherwise. Every full root stays full.
These exact membership tests return the original assignment in all
20,047 cases. The code also rechecks every closure against every carpet
implication. The twelve v_r are independently linearly independent;
the computed proper-carpet kernel ranks range from zero to fourteen.

## Independent controls and reproducibility

Direct sparse matrix multiplication in Python enumerates 219 selected
groups, independently of the native multiplication and kernel search.
The selection includes every valid assignment with at most two nonzero
roots, and one representative of every occurring group order at most
20,000. All 64,648 states, 108 root memberships per case, and predicted
group orders agree. Each actual reduction kernel is extracted from the
enumerated group and independently row-reduced: 3,421 kernel states in
total. The largest enumerated group has order 19,683. No truncated
enumeration is accepted. A second row-reduction implementation verifies
11,849 linear membership controls in dimensions two, three and sixteen.

The native C routine is ordinary integer matrix multiplication, with
entries reduced modulo three or nine; its largest accumulated entry is
at most 14 times 8 squared. It validates the modulus and entry ranges.
The saved binary agrees with pure Python on 128 fixed-seed dense products
and all 1,080 root group laws, including both moduli. The final auditor
checks every source hash, replays native controls, rebuilds the complete
input and every unit kernel in a fresh directory, reproduces every
closure, and repeats the independent matrix controls. Elapsed times are
excluded from mathematical equality, while original files remain hash
bound. Actual process observations and the final audit observation are
stored separately from mathematical output.

This is an exhaustive finite-ring result, not a proof over arbitrary
rings. The integral Chevalley model and the previous prime-field packet
remain explicit dependencies. Exact kernel arithmetic eliminates a
need to enumerate the largest lifted groups in full.
