# Problem 19.61: final candidate report

12 September 2026. Full affirmative candidate. Priority and outside
review remain pending.

The root closure of every elementary carpet is a carpet. Its derived
carpet equals the derived carpet of the original family. The proof is
in `19.61-proof.md` and the complete packet is
`results/19.61-square-completion-packet.json`.

The new algebraic step is that adjoining a^2 b at root r, for a in A_r
and b in A_-r, preserves the derived family. The exact 300 rank-two
certificates contain 3,191 nodes and cover both inputs of every ordered
Chevalley rule. Iterated adjoining gives a carpet C satisfying the
known square closedness criterion. The actual closure D lies between
A and C, while derived(C)=derived(A), forcing D to be a carpet.

The final 395-file replay passes (actual exit zero, tool chunk 379a54).
It reconstructs the targets by sparse polynomial multiplication,
checks every integer derivation, replays all rank-two root models and
polynomial commutator constants, and repeats 193,805 finite controls.
Twelve corruptions are rejected. The first-argument-only pilot is
retained but excluded as proof of complete coefficient coverage.
All sources, scripts, certificate files, logs and actual exit records
are committed locally. The known 19.63 criterion is explicitly credited;
its direct proof is available in `19.62-closedness-criterion.md`.

Finite completion enlarges 38,632 initial carpets. In 20,300 cases it
is strictly larger than the actual group closure. Thus the proof uses
an upper bound C; it never assumes the adjoined root elements already
belong to the original group. The earlier C2 and G2 search reports
remain preserved as bounded, independently verified controls.

This result and the separate 19.62 theorem are two distinct Notebook
candidate resolutions. Neither has external acceptance or established
priority. No messages or results have been sent outside the workspace.
